# RevenueCat core — SDK, purchase service, cached premium state

**Prompt:** `claude-prompts/2026-06-12/002-revenuecat-core.md`
**Completed:** 2026-06-12 · **Status:** done

## Summary

Added the RevenueCat plumbing the later paywall prompts will sit on — no UI and
no feature gating yet. The app now has a `PurchaseService` that knows whether the
user is premium, seeded instantly from an on-device cache (so it works offline
and on the first frame) and kept live by RevenueCat's customer-info listener.
Two Riverpod providers expose it (`purchaseServiceProvider`, `isPremiumProvider`),
`main.dart` configures the SDK in the background without blocking startup, and the
whole thing is mockable in tests via a reusable fake. Analyze is clean, all 55
tests pass, and a debug Android APK builds with the SDK linked in.

## Files touched

- `pubspec.yaml` / `pubspec.lock` — added `purchases_flutter` ^10.2.3 (latest stable).
- `lib/core/purchases/purchase_config.dart` (new) — placeholder API keys (clearly
  TODO-marked) + entitlement id `premium` and offering ids `onboarding`/`default`/
  `discount`.
- `lib/core/purchases/purchase_service.dart` (new) — `PurchaseService` interface,
  `RevenueCatPurchaseService` implementation, and the `PurchaseOutcome` enum
  (success / cancelled / error).
- `lib/core/purchases/purchase_providers.dart` (new) — the two providers.
- `lib/core/prefs/prefs_repository.dart` — added the `premiumCached` flag, following
  the existing typed-getter/setter pattern.
- `lib/main.dart` — creates the service, fire-and-forgets `init()` with error
  logging, and registers the provider override (next to prefs/db).
- `macos/Flutter/GeneratedPluginRegistrant.swift` — regenerated by `pub get` to
  register the new plugin (tracked file; committed for the macOS build).
- `test/support/fake_purchase_service.dart` (new) — reusable in-memory fake.
- `test/purchase_service_test.dart`, `test/purchase_providers_test.dart` (new).

## Decisions

- **Abstract `PurchaseService` + concrete RevenueCat impl + fake.** The interface
  lives in `lib/` so later paywall prompts and tests can inject a fake with
  `purchaseServiceProvider.overrideWithValue(...)`. The fake lives in `test/support/`
  so it's shared but doesn't bloat the shipped app.
- **Named the result enum `PurchaseOutcome`, not `PurchaseResult`.** RevenueCat
  10.x already exports a `PurchaseResult` type; reusing the name would collide.
  `success` / `cancelled` / `error` — cancellation is explicitly *not* an error.
- **Premium seeded synchronously in the constructor** from `prefs.premiumCached`
  (or `true` on unsupported platforms). So `isPremiumProvider` has the right answer
  on the very first frame, before any network call — the offline-first requirement.
- **Platform support is injectable** (`RevenueCatPurchaseService(prefs,
  supported: ...)`). Real builds auto-detect (Android/iOS/macOS supported; Windows/
  Linux/web/desktop-dev → premium unlocked per CLAUDE.md §3). The override lets the
  unit tests exercise both paths without real SDK calls.
- **Used the non-deprecated `Purchases.purchase(PurchaseParams.package(...))`** —
  `purchasePackage` is deprecated in 10.x.
- **Every SDK call is wrapped in try/catch.** A placeholder key or a network
  failure logs and continues with the cached status; it never crashes or blocks.

## Verification

```
$ flutter analyze
No issues found! (ran in 2.6s)

$ flutter test
All tests passed!   (55 tests; 10 new for purchases)

$ flutter build apk --debug
✓ Built build/app/outputs/flutter-apk/app-debug.apk
```

Self-correction: none needed for analyze/test (green first run). During writing I
fixed two analyzer findings before the verification run — the deprecated
`purchasePackage` call and an unused import — and dropped a `Package`-typed test
helper that couldn't be constructed cheaply, testing the same provider-update path
through `restorePurchases()` instead.
(The `drift` "database created multiple times" warning in the test log is
pre-existing harness noise, unrelated to this change.)

## Open items for the owner

- **API keys are placeholders** (`appl_TODO_…`, `goog_TODO_…` in
  `purchase_config.dart`). Paste the real public SDK keys from the RevenueCat
  dashboard before any real purchase can run.
- **Dashboard setup is still pending** (your side): create the 3 yearly products in
  App Store Connect / Play Console, map them in RevenueCat, and configure the 3
  offerings (`onboarding`, `default`, `discount`) and the `premium` entitlement —
  ids must match `purchase_config.dart`.
- iOS purchase testing needs the App Store sandbox; not exercised here (no UI yet).

## Deviations from prompt

None.
